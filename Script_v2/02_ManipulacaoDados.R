### Parte 2: Manipulação de dados ###
# O objetivo dessa parte do script é transformar o dataset

# Padronizar as variáveis numéricas do dataset
for(i in var.numericas) {
  # vendas[[i]] <- scale(vendas[[i]], center = 0)
}